stages:
- name: Publish image
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: php-example:${CICD_EXECUTION_SEQUENCE}
      registry: deltaapps/php
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deploy/deployment.yaml
